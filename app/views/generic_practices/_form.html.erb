<%= form_for(@generic_practice) do |f| %>
  <% if @generic_practice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@generic_practice.errors.count, "error") %> prohibited this generic_practice from being saved:</h2>

      <ul>
      <% @generic_practice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :summary %><br>
    <%= f.text_area :summary %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :generic_goal_id %><br>
    <%= f.text_field :generic_goal_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
