<%= form_for(@specific_practice_elaboration) do |f| %>
  <% if @specific_practice_elaboration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@specific_practice_elaboration.errors.count, "error") %> prohibited this specific_practice_elaboration from being saved:</h2>

      <ul>
      <% @specific_practice_elaboration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :process_area_id %><br>
    <%= f.number_field :process_area_id %>
  </div>
  <div class="field">
    <%= f.label :specific_practice_id %><br>
    <%= f.number_field :specific_practice_id %>
  </div>
  <div class="field">
    <%= f.label :sp_elaboration %><br>
    <%= f.text_area :sp_elaboration %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
