<%= form_for(@generic_practice_elaboration) do |f| %>
  <% if @generic_practice_elaboration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@generic_practice_elaboration.errors.count, "error") %> prohibited this generic_practice_elaboration from being saved:</h2>

      <ul>
      <% @generic_practice_elaboration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :process_area_id %><br>
    <%= f.number_field :process_area_id %>
  </div>
  <div class="field">
    <%= f.label :generic_practice_id %><br>
    <%= f.number_field :generic_practice_id %>
  </div>
  <div class="field">
    <%= f.label :gp_elaboration %><br>
    <%= f.text_area :gp_elaboration %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
