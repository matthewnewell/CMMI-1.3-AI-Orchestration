<h1>Generic Goals</h1>

<table>
  <tbody>
    <% @generic_goals.each do |generic_goal| %>
      <tr>
        <td><%= link_to generic_goal.name, generic_goal %></td>
      </tr>
      <tr>
        <td><%= markdown(generic_goal.description) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

