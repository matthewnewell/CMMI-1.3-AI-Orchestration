<%= form_for(@specific_goal) do |f| %>
  <% if @specific_goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@specific_goal.errors.count, "error") %> prohibited this specific_goal from being saved:</h2>

      <ul>
      <% @specific_goal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :process_area_id %><br>
    <%= f.number_field :process_area_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
